'use client';

import { OlusturulanSunum } from '@/types';

interface Props {
  data: OlusturulanSunum;
}

export default function Template2QuickSale({ data }: Props) {
  // Placeholder - Template2 will be implemented later
  return (
    <div className="min-h-screen bg-white p-8 text-center">
      <h1 className="text-4xl font-bold mb-4">Template 2: Quick Sale</h1>
      <p className="text-gray-600">Bu template yakında eklenecek.</p>
    </div>
  );
}

